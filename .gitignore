# Credenciales MCP (contiene API keys)
.mcp.json

# Basura de editor / sistema operativo
.DS_Store
Thumbs.db
*.log

# Documentos internos - NO deben subirse (repo publico)
docs/

# Repo de plugins de Claude Code clonado dentro del proyecto: no es parte del sitio
everything-claude-code/

# Fotos originales de camara (8-12 MB c/u). Se conservan en local como fuente,
# pero al sitio solo suben las versiones optimizadas de assets/img/menu/
_source/menuurban/

# Archivo fuente de Illustrator (3 MB, no lo usa el sitio)
_source/PersonajeUrbanFood.ai
